# dnsmasq.conf
interface={{ interface }}
bind-interfaces               # Bind only to explicit interface above
except-interface=lo           # Never run on localhost interface
listen-address={{ listen_address }}    # Local IP address to bind

# DNS
domain-needed                 # Prevent upstream lookups for plain names
bogus-priv                    # Block RFC1918 private address lookups
no-resolv                     # Ignore /etc/resolv.conf, use upstream servers below
{% if dns_upstream_1.to_string() == "::1" || dns_upstream_1.to_string() == "127.0.0.1" %}
cache-size=0                  # Do not cache results, defer to localhost resolver
{% endif %}
server={{ dns_upstream_1 }}                # Upstream DNS server
server={{ dns_upstream_2 }}                # Upstream DNS server
local=/{{ domain_lan }}/       # Authoritative local LAN domain name
domain={{ domain_lan }}        # Expand short hostnames for domain_lan
expand-hosts                  # Expand short hostnames found in /etc/hosts

# DHCP
dhcp-range={{ dhcp_lan_range_start }},{{ dhcp_lan_range_end }},{{ dhcp_lan_lease }}  # DHCP lease IP range start,end,duration
dhcp-authoritative                         # Tell clients we are in charge of this network
dhcp-option=option:router,{{ gateway_lan }}      # Tell clients to use this default gateway
dhcp-option=option:dns-server,{{ dns_lan }}  # Tell clients to use this default DNS server
